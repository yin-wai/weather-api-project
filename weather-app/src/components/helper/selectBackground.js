import cloud from '../../styles/images/cloud.jpg'

export const selectBackground = (cloudsProp, rainProp, snowProp) => {
    if (cloudsProp === 'cloudy') {
        return cloud 
    } else if () {

    }
}